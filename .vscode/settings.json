{
  "restoreTerminals.artificialDelayMilliseconds": 300,
  "restoreTerminals.terminals": [
    {
      "splitTerminals": [
        {
          "name": "server",
          "commands": ["npx json-server src/db.json -p 5000"]
        },
        {
          "name": "client",
          "commands": ["npm run start"]
        }
      ]
    }
  ]
}
